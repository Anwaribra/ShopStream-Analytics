name: 'shopstream_dwh'
version: '1.0.0'

profile: 'shopstream_dwh'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]




clean-targets:        
  - "target"
  - "dbt_packages"

models:
  shopstream_dwh:
   
    example:
      +materialized: view

    silver:
      +materialized: incremental
      +tags: ['silver']
    gold:
      +materialized: table
      +tags: ['gold']
